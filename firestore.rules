rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /products/{id} {
      allow read: if true;
      allow create, update, delete, write: if request.auth != null && request.auth.token.email == 'admin@cti-muebles.com';
    }
  }
}